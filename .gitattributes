# 统一行尾策略：仓库内一律存 LF，检出时由 git 按平台处理。
# 本机 core.autocrlf=true，每次 git add 都会对文本文件打印
# "LF will be replaced by CRLF" 警告 —— 这个警告会污染脚本输出，
# 在 PowerShell 里甚至会被当成错误记录。用显式规则收敛掉。

* text=auto eol=lf

# 图片、字体等二进制资源，明确排除任何行尾转换（避免损坏文件）
*.png  binary
*.jpg  binary
*.jpeg binary
*.webp binary
*.gif  binary
*.ico  binary
*.svg  text eol=lf
*.woff  binary
*.woff2 binary
*.ttf  binary
*.otf  binary

# 脚本：强制 LF，保证在 Git Bash / WSL / Linux 下可直接执行
*.sh   text eol=lf
*.ps1  text eol=crlf
*.bat  text eol=crlf

# 文本资源
*.html text eol=lf
*.css  text eol=lf
*.js   text eol=lf
*.json text eol=lf
*.md   text eol=lf
